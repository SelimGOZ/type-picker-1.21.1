package net.fortuneblack05.typerando.payloads;

import net.minecraft.util.Identifier;

public final class TypePickerPayloads {
    private TypePickerPayloads() {}

    public static final Identifier SPIN_RESULT_ID = Identifier.of("typerando", "spin_result");
    public static final Identifier ROLL_SUMMARY_ID = Identifier.of("typerando", "roll_summary");
}